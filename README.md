# 个人网站项目

一个使用 React、React Router 和 Tailwind CSS 构建的现代化个人网站。

## 🌟 特性

- **响应式设计** - 适配各种设备和屏幕尺寸
- **现代化UI** - 使用 Tailwind CSS 构建美观的用户界面
- **单页应用** - 使用 React Router 实现流畅的页面切换
- **组件化架构** - 可复用的 React 组件
- **优化性能** - 快速加载和流畅的用户体验

## 📋 页面结构

### 🏠 首页 (Home)
- 个人介绍和欢迎信息
- 技能特色展示
- 精选项目预览
- 行动号召区域

### 👨‍💻 关于我 (About)
- 个人简介和职业经历
- 技能专长展示
- 职业时间线
- 个人理念和联系方式

### 💼 项目作品集 (Portfolio)
- 项目展示和分类筛选
- 项目详细信息和技术栈
- GitHub 和演示链接
- 技术统计

### 📝 博客 (Blog)
- 自动跳转到外部博客 (blog.jlands.cn)
- 加载动画和用户友好的跳转页面
- 手动跳转选项

## 🛠️ 技术栈

- **前端框架**: React 18
- **路由管理**: React Router DOM v6
- **样式框架**: Tailwind CSS
- **构建工具**: Create React App
- **包管理**: npm

## 🚀 快速开始

### 环境要求

- Node.js (版本 14 或更高)
- npm 或 yarn

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm start
```

项目将在 [http://localhost:3000](http://localhost:3000) 启动。

### 构建生产版本

```bash
npm run build
```

构建文件将生成在 `build` 目录中。

### 运行测试

```bash
npm test
```

## 📁 项目结构

```
my-web/
├── public/
│   ├── index.html
│   └── ...
├── src/
│   ├── components/
│   │   ├── Navbar.js      # 导航栏组件
│   │   └── Footer.js      # 页脚组件
│   ├── pages/
│   │   ├── Home.js        # 首页
│   │   ├── About.js       # 关于页面
│   │   ├── Portfolio.js   # 项目作品集
│   │   └── Blog.js        # 博客页面
│   ├── App.js             # 主应用组件
│   ├── index.js           # 应用入口
│   └── index.css          # 全局样式
├── package.json
├── tailwind.config.js     # Tailwind 配置
├── postcss.config.js      # PostCSS 配置
└── README.md
```

## 🎨 自定义配置

### 修改个人信息

1. **导航栏标题**: 编辑 `src/components/Navbar.js` 中的网站名称
2. **个人信息**: 修改各页面组件中的个人信息
3. **联系方式**: 更新 `src/components/Footer.js` 和 `src/pages/About.js` 中的联系信息
4. **博客链接**: 修改 `src/pages/Blog.js` 中的博客地址

### 添加项目

在 `src/pages/Portfolio.js` 中的 `projects` 数组添加新项目：

```javascript
{
  id: 7,
  title: '项目名称',
  description: '项目描述',
  image: '项目图片URL',
  category: 'fullstack', // 或 'frontend', 'backend', 'mobile'
  technologies: ['React', 'Node.js'],
  github: 'GitHub链接',
  demo: '演示链接',
  featured: false // 是否为精选项目
}
```

### 自定义样式

- **主题颜色**: 修改 `tailwind.config.js` 中的颜色配置
- **字体**: 在 `tailwind.config.js` 中配置字体家族
- **自定义组件**: 在 `src/index.css` 中添加自定义 CSS 类

## 🌐 部署
### 在线预览

 `https://web.jlands.cn/`
 
### Vercel 部署

1. 将代码推送到 GitHub
2. 在 Vercel 中导入项目
3. 自动部署完成

### Netlify 部署

1. 运行 `npm run build`
2. 将 `build` 目录上传到 Netlify
3. 配置重定向规则（用于 SPA 路由）

### GitHub Pages 部署

1. 安装 gh-pages: `npm install --save-dev gh-pages`
2. 在 package.json 添加部署脚本:
   ```json
   "scripts": {
     "predeploy": "npm run build",
     "deploy": "gh-pages -d build"
   }
   ```
3. 运行 `npm run deploy`

## 📱 响应式设计

网站采用移动优先的响应式设计：

- **移动设备** (< 768px): 单列布局，折叠导航
- **平板设备** (768px - 1024px): 两列布局
- **桌面设备** (> 1024px): 多列布局，完整功能

## 🔧 开发建议

1. **代码规范**: 使用 ESLint 和 Prettier 保持代码一致性
2. **组件复用**: 将重复的 UI 元素提取为可复用组件
3. **性能优化**: 使用 React.memo 和 useMemo 优化性能
4. **SEO优化**: 添加适当的 meta 标签和结构化数据
